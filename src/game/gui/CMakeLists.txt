
SET (GUI_SRC
  ApiDefinitions.h
)

INCLUDE_DIRECTORIES(
  ${CMAKE_CURRENT_SOURCE_DIR}
  ${GAME_INCLUDE_DIR}
  ${GLOBAL_INCLUDE_DIR}
)

ADD_DEFINITIONS("-DGUI_API_EXPORTS")

ADD_LIBRARY(TRBGUI SHARED
  ${GUI_SRC}
)

INSTALL(TARGETS TRBGUI RUNTIME DESTINATION ${TRB_INSTALLATION_PREFIX})